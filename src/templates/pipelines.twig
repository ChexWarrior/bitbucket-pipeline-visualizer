{# TESTING ONLY  #}
{% for i in 0..7 %}
  <div class="pipeline-info column is-one-third">
    <div class="box">
      <span class="title is-5">Test Pipeline {{ i }}</span>
      <div class="pipeline-steps is-hidden">
        {% for j in 1..random(2, 5) %}
          <div>Step {{j}}</div>
        {% endfor %}
      </div>
    </div>
  </div>
{% endfor %}
